# Puerto del servidor
PORT=3000

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wesapp
DB_USER=tu_usuario
DB_PASSWORD=tu_password

# JWT Secret (cambiar en producción)
JWT_SECRET=tu_secreto_super_seguro_cambialo_2024

# JWT Expiration
JWT_EXPIRATION=24h

# Ambiente
NODE_ENV=development

# CORS Origin
CORS_ORIGIN=*